# qt-puppycam

Headless Qt 6 (QtCore + QtNetwork) webcam streaming service for Linux/Raspberry Pi.

## Build (Debian 13)

Install deps:
```bash
sudo apt update
sudo apt install -y build-essential cmake ninja-build pkg-config qt6-base-dev qt6-base-dev-tools
